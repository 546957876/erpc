# Superseded Admin Web Plan

This plan described an embedded Admin UI and runtime CRUD implemented inside
eRPC. That direction remains rejected. The independent `Admin/` and `web/`
applications now continue with PostgreSQL-backed desired configuration and
Admin-owned process control in `2026-08-26-admin-managed-erpc.md`; eRPC itself
remains unchanged.
