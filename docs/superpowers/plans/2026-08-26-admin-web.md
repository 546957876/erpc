# Superseded Admin Web Plan

This plan described an embedded Admin UI and dynamic eRPC upstream CRUD. That
direction was rejected. The implementation now follows
`docs/superpowers/plans/2026-08-26-admin-control-plane.md`: an independent
`Admin/` Go process and an independent `web/` React app that proxy the existing
eRPC admin API without changing eRPC.
